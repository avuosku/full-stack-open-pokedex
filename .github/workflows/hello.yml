name: Hello World

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello World!"

      - name: Show Date
        run: date

      - name: List Directory Contents
        run: ls -l